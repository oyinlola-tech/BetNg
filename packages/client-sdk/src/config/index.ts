/**
 * @betng/client-sdk/config
 *
 * Where a client finds the platform.
 */

export { DEFAULT_TIMEOUT_MS } from "./clientConfig.type.js";
export type { BetNgClientConfig } from "./clientConfig.type.js";
