{
  "name": "@betng/ui-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Framework-free presentation logic shared by web, mobile and TV: the view models a screen renders, match-phase derivation, formatters for money, odds and the match clock, standings and bet-slip arithmetic.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@betng/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
