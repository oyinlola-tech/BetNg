import { Glyph } from "./Glyph";
import type { FootballIconProps } from "./IconBase";

export function FreeKick(props: FootballIconProps): React.JSX.Element {
  return <Glyph name="freeKick" {...props} />;
}
