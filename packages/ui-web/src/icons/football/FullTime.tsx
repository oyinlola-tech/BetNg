import { Glyph } from "./Glyph";
import type { FootballIconProps } from "./IconBase";

export function FullTime(props: FootballIconProps): React.JSX.Element {
  return <Glyph name="fullTime" {...props} />;
}
