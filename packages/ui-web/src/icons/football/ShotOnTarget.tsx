import { Glyph } from "./Glyph";
import type { FootballIconProps } from "./IconBase";

export function ShotOnTarget(props: FootballIconProps): React.JSX.Element {
  return <Glyph name="shotOnTarget" {...props} />;
}
