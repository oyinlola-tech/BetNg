[project]
name = "betng-analytics"
version = "0.1.0"
description = "BetNG analytics service — read-only global bet analysis and reports, every figure a SQL aggregate over database rows."
requires-python = ">=3.12"
dependencies = [
    "betng-service-kit",
    "fastapi>=0.120",
    "pydantic>=2.10",
    "uvicorn[standard]>=0.34",
]

[project.optional-dependencies]
dev = [
    "httpx>=0.28",
    "mypy>=1.14",
    "pytest>=8.3",
    "pytest-asyncio>=0.25",
    "ruff>=0.9",
]

[project.scripts]
betng-analytics = "betng_analytics.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/betng_analytics"]

[tool.uv.sources]
betng-service-kit = { path = "../shared", editable = true }

[tool.ruff]
line-length = 88
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "A", "C4", "SIM", "RUF", "D"]
# D1xx (missing docstring) is off: comments are kept to where they are needed.
ignore = ["D100", "D101", "D102", "D103", "D104", "D105", "D107", "D203", "D213"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["D"]

[tool.mypy]
python_version = "3.12"
strict = true
mypy_path = "src"
packages = ["betng_analytics"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
