import type { RouteObject } from "react-router";

/** Wallet, payments, bank accounts, statements, KYC and responsible gaming. Mounted under the app shell. */
export const moneyRoutes: RouteObject[] = [];
