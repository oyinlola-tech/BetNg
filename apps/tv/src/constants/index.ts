/**
 * @betng/tv/constants
 *
 * Fixed values this client renders with. The domain's own constants — the
 * API prefix, the request-id header, the error codes — come from
 * `@betng/contracts` so the client and the services cannot disagree.
 */

export { API_PREFIX, ErrorCodes, REQUEST_ID_HEADER } from "@betng/contracts";
