/**
 * @betng/gateway/services
 *
 * The gateway's one operation: forwarding to the service that owns the data.
 */

export { forward, upstreamPath } from "./gateway/index.js";
export type { ForwardOptions } from "./gateway/index.js";
