/**
 * @betng/gateway/configs
 *
 * Environment-backed configuration for the gateway.
 */

export {
  DEFAULT_PORT,
  loadGatewayConfig,
  SERVICE_NAME,
  SERVICE_VERSION,
} from "./service.config.js";
