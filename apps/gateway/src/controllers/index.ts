/**
 * @betng/gateway/controllers
 *
 * The gateway's forwarding handlers.
 */

export { proxyRead, proxyWrite } from "./proxy.controller.js";
export type { ProxyHandler } from "./proxy.controller.js";
