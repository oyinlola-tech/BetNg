/**
 * @betng/mobile/store
 *
 * Client-side state shared across screens. The two screens in this phase
 * hold their own state in hooks; a store arrives when something genuinely
 * spans them — the bet slip, which is built up across markets.
 */

export {};
