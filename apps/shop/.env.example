# Where the terminal reads from: "mock" runs a virtual season and a demo shop in the browser,
# "platform" talks to the gateway and event service below.
VITE_DATA_SOURCE=mock
VITE_GATEWAY_URL=http://localhost:3000
VITE_LIVE_URL=ws://localhost:3008/live
# Only used by the shared match data source for account reads the terminal never makes.
VITE_USER_ID=11111111-1111-4111-8111-111111111111
