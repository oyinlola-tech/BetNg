/**
 * Domain events. The betting service publishes none: risk, settlement and
 * analytics read accepted bets from the `betting` schema, which is the
 * authority, rather than from a stream that could drop one.
 */

export {};
