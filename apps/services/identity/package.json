{
  "name": "@betng/identity-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BetNG identity service \u2014 customers, admin users, shops, cashiers, sessions, RBAC, audit log and platform settings.",
  "zudojs": {
    "features": [
      "database"
    ]
  },
  "scripts": {
    "bootstrap:super-admin": "tsx src/seeds/bootstrapSuperAdmin.cli.ts",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy && tsx src/seeds/bootstrapSuperAdmin.cli.ts",
    "db:migrate:dev": "prisma migrate dev",
    "db:migrate:only": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "totp:dev": "tsx src/seeds/seedTotp.cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@betng/contracts": "workspace:*",
    "@betng/service-kit": "workspace:*",
    "@prisma/adapter-pg": "7.10.0",
    "@prisma/client": "7.10.0",
    "@zudojs/container": "1.1.2",
    "@zudojs/cqrs": "1.1.1",
    "@zudojs/crypto": "1.3.0",
    "@zudojs/database": "1.2.1",
    "@zudojs/errors": "1.2.0",
    "@zudojs/events": "1.2.0",
    "@zudojs/rpc": "1.3.0",
    "@zudojs/validation": "1.0.3"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "prisma": "7.10.0",
    "tsx": "^4.23.15",
    "typescript": "7.0.2"
  }
}
