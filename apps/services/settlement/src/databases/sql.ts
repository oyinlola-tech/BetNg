// The generated client's `Prisma.sql` loses its type under pnpm (its runtime typings import a package only
// `@prisma/client` can resolve), so the same helpers are taken from `@prisma/client`, fully typed.
export { join, sqltag as sql } from "@prisma/client/runtime/client";
export type { Sql } from "@prisma/client/runtime/client";
