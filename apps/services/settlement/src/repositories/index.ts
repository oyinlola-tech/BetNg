/**
 * @betng/settlement-service/repositories
 *
 * Concrete implementations of the settlement data-access contract.
 */

export { createInMemorySettlementRepository } from "./settlement.repository.js";
