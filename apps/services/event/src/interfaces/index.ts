/**
 * @betng/event-service/interfaces
 *
 * The contracts the event service's layers are written against.
 */

export type { ChannelRegistry, ChannelState } from "./event.interface.js";
export type { LiveEvent, ServerFrame, WebSocketSession } from "./event.types.js";
