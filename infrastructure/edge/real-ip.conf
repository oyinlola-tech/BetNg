# The edge is the first hop by default, so $remote_addr is the client and nothing here is trusted.
# Behind a load balancer, list only its addresses; X-Forwarded-For from anyone else is ignored.
# set_real_ip_from 10.0.0.0/8;
# real_ip_header X-Forwarded-For;
# real_ip_recursive on;
